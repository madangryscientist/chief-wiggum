# Test Prompt for E2E Testing

Create a file called `test-output.txt` in the workspace root with the content:

```
Hello from chief-wiggum test!
Timestamp: [current timestamp]
```

After creating the file, verify it exists and contains the expected content.

When complete, output: COMPLETE
